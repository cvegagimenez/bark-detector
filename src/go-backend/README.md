# Go Backend

This directory contains the Go backend for the Bark Detector project.

## Structure

- `cmd/server/`: Entry point for the server application.
- `internal/`: Internal Go packages.
- `pkg/`: Reusable Go packages.
